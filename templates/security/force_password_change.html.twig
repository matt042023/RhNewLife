{% extends 'layouts/auth.html.twig' %}

{% block title %}Changement de mot de passe obligatoire - RH NewLife{% endblock %}

{% block page_title %}Changement de mot de passe{% endblock %}

{% block page_subtitle %}
    <p class="mt-2 text-sm text-gray-600">Vous devez définir un nouveau mot de passe pour continuer</p>
{% endblock %}

{% block content %}
    {# Info box #}
    <div class="mb-6 p-4 bg-amber-50 border border-amber-200 rounded-lg">
        <div class="flex gap-3">
            <svg class="w-5 h-5 text-amber-600 flex-shrink-0 mt-0.5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 9v2m0 4h.01m-6.938 4h13.856c1.54 0 2.502-1.667 1.732-3L13.732 4c-.77-1.333-2.694-1.333-3.464 0L3.34 16c-.77 1.333.192 3 1.732 3z" />
            </svg>
            <div class="text-sm text-amber-800">
                <p class="font-semibold">Mot de passe temporaire</p>
                <p>Votre compte a été créé avec un mot de passe temporaire. Pour des raisons de sécurité, vous devez le changer maintenant.</p>
            </div>
        </div>
    </div>

    {% if errors is not empty %}
        <div class="alert alert-error mb-4">
            <svg class="w-5 h-5" fill="currentColor" viewBox="0 0 20 20">
                <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zM8.707 7.293a1 1 0 00-1.414 1.414L8.586 10l-1.293 1.293a1 1 0 101.414 1.414L10 11.414l1.293 1.293a1 1 0 001.414-1.414L11.414 10l1.293-1.293a1 1 0 00-1.414-1.414L10 8.586 8.707 7.293z" clip-rule="evenodd"/>
            </svg>
            <ul class="list-disc list-inside">
                {% for error in errors %}
                    <li>{{ error }}</li>
                {% endfor %}
            </ul>
        </div>
    {% endif %}

    <form method="post" class="space-y-6" data-controller="password-strength">
        <div>
            <label for="new_password" class="form-label">
                Nouveau mot de passe
            </label>
            <input
                type="password"
                name="new_password"
                id="new_password"
                class="form-input"
                required
                autofocus
                minlength="8"
                placeholder="Minimum 8 caractères"
                data-password-strength-target="input"
                data-action="input->password-strength#check"
            >
            <div class="mt-2" data-password-strength-target="meter">
                <div class="flex gap-1 mb-1">
                    <div class="h-1 flex-1 rounded-full bg-gray-200" data-password-strength-target="bar1"></div>
                    <div class="h-1 flex-1 rounded-full bg-gray-200" data-password-strength-target="bar2"></div>
                    <div class="h-1 flex-1 rounded-full bg-gray-200" data-password-strength-target="bar3"></div>
                    <div class="h-1 flex-1 rounded-full bg-gray-200" data-password-strength-target="bar4"></div>
                </div>
                <p class="text-xs text-gray-500" data-password-strength-target="text">Entrez un mot de passe</p>
            </div>
        </div>

        <div>
            <label for="confirm_password" class="form-label">
                Confirmer le mot de passe
            </label>
            <input
                type="password"
                name="confirm_password"
                id="confirm_password"
                class="form-input"
                required
                minlength="8"
                placeholder="Retapez le mot de passe"
            >
        </div>

        <div class="text-sm text-gray-600 bg-gray-50 p-3 rounded-lg">
            <p class="font-medium mb-2">Votre mot de passe doit contenir :</p>
            <ul class="space-y-1">
                <li class="flex items-center gap-2">
                    <svg class="w-4 h-4 text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7" />
                    </svg>
                    Au moins 8 caractères
                </li>
                <li class="flex items-center gap-2">
                    <svg class="w-4 h-4 text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7" />
                    </svg>
                    Idéalement des majuscules, minuscules et chiffres
                </li>
            </ul>
        </div>

        <div>
            <button type="submit" class="btn btn-primary w-full justify-center">
                <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 7a2 2 0 012 2m4 0a6 6 0 01-7.743 5.743L11 17H9v2H7v2H4a1 1 0 01-1-1v-2.586a1 1 0 01.293-.707l5.964-5.964A6 6 0 1121 9z" />
                </svg>
                Changer mon mot de passe
            </button>
        </div>
    </form>

    <div class="mt-6 text-center">
        <a href="{{ path('app_logout') }}" class="text-sm text-gray-600 hover:text-gray-900">
            Se déconnecter
        </a>
    </div>
{% endblock %}

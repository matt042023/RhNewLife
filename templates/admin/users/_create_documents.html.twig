{# Step 2: Documents #}
<div class="space-y-8">
    <div class="flex items-center justify-between">
        <h2 class="text-xl font-bold text-gray-900 dark:text-white flex items-center gap-2">
            <div class="w-8 h-8 glass-card glass-card-success rounded-lg flex items-center justify-center">
                <svg class="w-5 h-5 text-green-600 dark:text-green-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12h6m-6 4h6m2 5H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z" />
                </svg>
            </div>
            Documents
        </h2>

        {# Progress indicator #}
        <div class="flex items-center gap-2" data-direct-user-creation-target="documentsProgress">
            <span class="text-sm text-gray-600 dark:text-gray-400">
                <span data-direct-user-creation-target="documentsCount">0</span>/4 documents
            </span>
            <div class="w-24 h-2 bg-gray-200 dark:bg-gray-700 rounded-full overflow-hidden">
                <div class="h-full bg-green-500 transition-all duration-300"
                     data-direct-user-creation-target="documentsProgressBar"
                     style="width: 0%"></div>
            </div>
        </div>
    </div>

    {# Documents required option #}
    <div class="flex items-center gap-3 p-4 glass-card glass-card-primary">
        {# Hidden input to ensure value is always sent (unchecked checkbox sends nothing) #}
        <input type="hidden" name="documents_required" value="0">
        <input type="checkbox" name="documents_required" value="1" id="documents_required"
               class="w-5 h-5 text-blue-600 dark:text-blue-400 rounded focus:ring-2 focus:ring-blue-500"
               {{ (formData.documents_required ?? '1') == '1' ? 'checked' : '' }}
               data-direct-user-creation-target="documentsRequired"
               data-action="change->direct-user-creation#toggleDocumentsRequired">
        <label for="documents_required" class="text-sm text-gray-700 dark:text-gray-300 font-medium cursor-pointer">
            Rendre les documents obligatoires
        </label>
        <span class="text-xs text-gray-500 dark:text-gray-400 ml-2">
            (Décochez si vous voulez créer le salarié sans documents)
        </span>
    </div>

    {# Documents grid #}
    <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
        {% set documentTypes = [
            {id: 'cni', label: "Carte d'identité", icon: 'id-card', required: true},
            {id: 'rib', label: 'RIB', icon: 'credit-card', required: true},
            {id: 'domicile', label: 'Justificatif de domicile', icon: 'home', required: true},
            {id: 'honorabilite', label: "Attestation d'honorabilité", icon: 'shield-check', required: true},
            {id: 'diplome', label: 'Diplôme', icon: 'academic-cap', required: false}
        ] %}

        {% for doc in documentTypes %}
            <div class="document-upload-card p-4 bg-white/5 rounded-xl border border-white/10 hover:border-white/20 transition-all"
                 data-direct-user-creation-target="documentCard"
                 data-document-type="{{ doc.id }}"
                 data-required="{{ doc.required ? '1' : '0' }}">

                <div class="flex items-start justify-between mb-3">
                    <div class="flex items-center gap-2">
                        <div class="w-8 h-8 rounded-lg bg-gray-100 dark:bg-gray-700 flex items-center justify-center">
                            {% if doc.icon == 'id-card' %}
                                <svg class="w-4 h-4 text-gray-600 dark:text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 6H5a2 2 0 00-2 2v9a2 2 0 002 2h14a2 2 0 002-2V8a2 2 0 00-2-2h-5m-4 0V5a2 2 0 114 0v1m-4 0a2 2 0 104 0m-5 8a2 2 0 100-4 2 2 0 000 4zm0 0c1.306 0 2.417.835 2.83 2M9 14a3.001 3.001 0 00-2.83 2M15 11h3m-3 4h2" />
                                </svg>
                            {% elseif doc.icon == 'credit-card' %}
                                <svg class="w-4 h-4 text-gray-600 dark:text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 10h18M7 15h1m4 0h1m-7 4h12a3 3 0 003-3V8a3 3 0 00-3-3H6a3 3 0 00-3 3v8a3 3 0 003 3z" />
                                </svg>
                            {% elseif doc.icon == 'home' %}
                                <svg class="w-4 h-4 text-gray-600 dark:text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 12l2-2m0 0l7-7 7 7M5 10v10a1 1 0 001 1h3m10-11l2 2m-2-2v10a1 1 0 01-1 1h-3m-6 0a1 1 0 001-1v-4a1 1 0 011-1h2a1 1 0 011 1v4a1 1 0 001 1m-6 0h6" />
                                </svg>
                            {% elseif doc.icon == 'shield-check' %}
                                <svg class="w-4 h-4 text-gray-600 dark:text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m5.618-4.016A11.955 11.955 0 0112 2.944a11.955 11.955 0 01-8.618 3.04A12.02 12.02 0 003 9c0 5.591 3.824 10.29 9 11.622 5.176-1.332 9-6.03 9-11.622 0-1.042-.133-2.052-.382-3.016z" />
                                </svg>
                            {% elseif doc.icon == 'academic-cap' %}
                                <svg class="w-4 h-4 text-gray-600 dark:text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                    <path d="M12 14l9-5-9-5-9 5 9 5z" />
                                    <path d="M12 14l6.16-3.422a12.083 12.083 0 01.665 6.479A11.952 11.952 0 0012 20.055a11.952 11.952 0 00-6.824-2.998 12.078 12.078 0 01.665-6.479L12 14z" />
                                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 14l9-5-9-5-9 5 9 5zm0 0l6.16-3.422a12.083 12.083 0 01.665 6.479A11.952 11.952 0 0012 20.055a11.952 11.952 0 00-6.824-2.998 12.078 12.078 0 01.665-6.479L12 14zm-4 6v-7.5l4-2.222" />
                                </svg>
                            {% endif %}
                        </div>
                        <div>
                            <span class="font-medium text-gray-800 dark:text-gray-200">{{ doc.label }}</span>
                            {% if doc.required %}
                                <span class="text-red-500 ml-1 document-required-marker">*</span>
                            {% endif %}
                        </div>
                    </div>

                    {# Status indicator #}
                    <div class="document-status">
                        <span class="document-status-empty inline-flex items-center px-2 py-1 rounded-full text-xs font-medium bg-gray-100 dark:bg-gray-700 text-gray-600 dark:text-gray-400">
                            Non fourni
                        </span>
                        <span class="document-status-ready hidden inline-flex items-center px-2 py-1 rounded-full text-xs font-medium bg-green-100 dark:bg-green-900/30 text-green-700 dark:text-green-400">
                            <svg class="w-3 h-3 mr-1" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7" />
                            </svg>
                            Prêt
                        </span>
                    </div>
                </div>

                {# Upload zone #}
                <div class="document-upload-zone relative">
                    <input type="file" name="document_{{ doc.id }}" id="document_{{ doc.id }}"
                           accept=".pdf,.jpg,.jpeg,.png"
                           class="absolute inset-0 w-full h-full opacity-0 cursor-pointer z-10"
                           data-action="change->direct-user-creation#handleFileSelect">

                    <div class="document-drop-area border-2 border-dashed border-gray-300 dark:border-gray-600 rounded-lg p-4 text-center hover:border-blue-400 dark:hover:border-blue-500 transition-colors">
                        <svg class="w-8 h-8 mx-auto text-gray-400 mb-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M7 16a4 4 0 01-.88-7.903A5 5 0 1115.9 6L16 6a5 5 0 011 9.9M15 13l-3-3m0 0l-3 3m3-3v12" />
                        </svg>
                        <p class="text-sm text-gray-500 dark:text-gray-400">
                            Glissez un fichier ou <span class="text-blue-500">cliquez pour parcourir</span>
                        </p>
                        <p class="text-xs text-gray-400 dark:text-gray-500 mt-1">PDF, JPEG ou PNG (max 10 Mo)</p>
                    </div>

                    {# Preview zone (hidden by default) #}
                    <div class="document-preview hidden mt-2 p-2 bg-gray-50 dark:bg-gray-800 rounded-lg">
                        <div class="flex items-center justify-between">
                            <div class="flex items-center gap-2">
                                <svg class="w-5 h-5 text-gray-500" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12h6m-6 4h6m2 5H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z" />
                                </svg>
                                <span class="document-filename text-sm text-gray-700 dark:text-gray-300 truncate max-w-[150px]"></span>
                            </div>
                            <button type="button" class="text-red-500 hover:text-red-700 p-1"
                                    data-action="click->direct-user-creation#removeFile"
                                    data-document-type="{{ doc.id }}">
                                <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12" />
                                </svg>
                            </button>
                        </div>
                    </div>
                </div>
            </div>
        {% endfor %}
    </div>

    {# Info #}
    <div class="glass-card glass-card-primary p-4">
        <div class="flex gap-3">
            <svg class="w-5 h-5 text-blue-600 dark:text-blue-400 flex-shrink-0 mt-0.5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 16h-1v-4h-1m1-4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z" />
            </svg>
            <div class="text-sm text-blue-800 dark:text-blue-200">
                <p class="font-semibold mb-1">Documents auto-validés</p>
                <p>Les documents uploadés ici seront automatiquement marqués comme validés puisqu'ils sont ajoutés par l'administrateur.</p>
            </div>
        </div>
    </div>
</div>

{% extends 'layout/dashboard_base.html.twig' %}

{% block title %}Demander un rendez-vous{% endblock %}

{% block page_header %}
<div class="px-4 py-6 lg:px-6">
    <div class="flex items-center mb-2">
        <a href="{{ path('app_appointment_index') }}" class="text-white/80 hover:text-white mr-3">
            <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 19l-7-7m0 0l7-7m-7 7h18"/>
            </svg>
        </a>
        <h1 class="text-2xl md:text-3xl font-bold text-white">Demander un rendez-vous</h1>
    </div>
    <p class="text-blue-100 text-sm md:text-base">Remplissez le formulaire ci-dessous pour envoyer une demande de rendez-vous</p>
</div>
{% endblock %}

{% block dashboard_content %}
<div class="max-w-3xl mx-auto">

    {# Formulaire #}
    <div class="bg-white dark:bg-gray-800 rounded-lg shadow-md p-6">
        {{ form_start(form, {'attr': {'data-controller': 'appointment-form'}}) }}

        {# Objet #}
        <div class="mb-6">
            {{ form_label(form.subject, null, {'label_attr': {'class': 'block text-sm font-medium text-gray-700 dark:text-gray-300 mb-2'}}) }}
            {{ form_widget(form.subject) }}
            {{ form_errors(form.subject) }}
            <p class="mt-1 text-sm text-gray-500 dark:text-gray-400">
                Indiquez le motif de votre demande de rendez-vous
            </p>
        </div>

        {# Destinataire #}
        <div class="mb-6">
            {{ form_label(form.recipient, null, {'label_attr': {'class': 'block text-sm font-medium text-gray-700 dark:text-gray-300 mb-2'}}) }}
            {{ form_widget(form.recipient) }}
            {{ form_errors(form.recipient) }}
            <p class="mt-1 text-sm text-gray-500 dark:text-gray-400">
                Sélectionnez l'administrateur ou directeur avec qui vous souhaitez avoir un rendez-vous
            </p>
        </div>

        {# Date souhaitée #}
        <div class="mb-6">
            {{ form_label(form.preferredDate, null, {'label_attr': {'class': 'block text-sm font-medium text-gray-700 dark:text-gray-300 mb-2'}}) }}
            {{ form_widget(form.preferredDate) }}
            {{ form_errors(form.preferredDate) }}
            {{ form_help(form.preferredDate) }}
        </div>

        {# Message #}
        <div class="mb-6">
            {{ form_label(form.description, null, {'label_attr': {'class': 'block text-sm font-medium text-gray-700 dark:text-gray-300 mb-2'}}) }}
            {{ form_widget(form.description) }}
            {{ form_errors(form.description) }}
            <p class="mt-1 text-sm text-gray-500 dark:text-gray-400">
                Ajoutez des informations complémentaires sur votre demande
            </p>
        </div>

        {# Informations importantes #}
        <div class="mb-6 p-4 bg-blue-50 dark:bg-blue-900/20 border border-blue-200 dark:border-blue-800 rounded-lg">
            <div class="flex">
                <svg class="w-5 h-5 text-blue-600 dark:text-blue-400 mr-3 flex-shrink-0 mt-0.5" fill="currentColor" viewBox="0 0 20 20">
                    <path fill-rule="evenodd" d="M18 10a8 8 0 11-16 0 8 8 0 0116 0zm-7-4a1 1 0 11-2 0 1 1 0 012 0zM9 9a1 1 0 000 2v3a1 1 0 001 1h1a1 1 0 100-2v-3a1 1 0 00-1-1H9z" clip-rule="evenodd"/>
                </svg>
                <div>
                    <h3 class="text-sm font-medium text-blue-800 dark:text-blue-200 mb-1">À savoir</h3>
                    <ul class="text-sm text-blue-700 dark:text-blue-300 list-disc list-inside space-y-1">
                        <li>Votre demande sera envoyée au destinataire pour validation</li>
                        <li>Vous recevrez une notification par email de la décision</li>
                        <li>La date proposée peut être modifiée par le destinataire</li>
                    </ul>
                </div>
            </div>
        </div>

        {# Boutons d'action #}
        <div class="flex gap-3">
            <button type="submit" class="flex-1 px-6 py-3 bg-blue-600 hover:bg-blue-700 text-white font-medium rounded-lg transition">
                <svg class="inline w-5 h-5 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 19l9 2-9-18-9 18 9-2zm0 0v-8"/>
                </svg>
                Envoyer la demande
            </button>
            <a href="{{ path('app_appointment_index') }}" class="px-6 py-3 bg-gray-200 hover:bg-gray-300 dark:bg-gray-700 dark:hover:bg-gray-600 text-gray-700 dark:text-gray-300 font-medium rounded-lg transition">
                Annuler
            </a>
        </div>

        {{ form_end(form) }}
    </div>
</div>
{% endblock %}
